
document.addEventListener('DOMContentLoaded', () => {
  const container = document.querySelector('[data-behavior="map"]')

  if (container) {
    container.innerHTML = "<h1><code>insert map here</code></h1>"
  }
})

